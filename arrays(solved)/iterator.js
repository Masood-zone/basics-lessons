//  CHALLENGE 1
const names = ["Ama", "Kwesi", "Yaw", "Abena"];

names.forEach(name => {
  console.log(`Hello, ${name}`);
});
 

// CHALLENGE 2
 const currency = ['10', '20' , '30' , '40']

currency.forEach(currency => { 
console.log('GHC' + currency);
});


// CHALLENGE 3
const items = ["book", "pen", "bag"];

const uppercased = items.map(item => item.toUpperCase());

console.log(uppercased);

