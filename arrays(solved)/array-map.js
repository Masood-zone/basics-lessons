// Challenge 1
const items = ["book", "pen", "bag"];

const uppercased = items.map((item) => item.toUpperCase());

console.log(uppercased);

// Challenge 2
const nums = [1, 2, 3, 4];
const doubled = nums.map((number) => number * number);
console.log(doubled);
